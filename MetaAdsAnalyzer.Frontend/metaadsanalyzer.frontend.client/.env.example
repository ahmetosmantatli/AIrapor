# İsteğe bağlı: doğrudan tam API kökü (sonda /api YOK — yollar zaten /api/... ile başlar).
# Yanlış: http://localhost:5195/api → istek /api/meta/... olunca /api/api/... olur (404).
# Doğru: http://localhost:5195 veya boş (Vite proxy).
# VITE_API_BASE_URL=http://localhost:5195
